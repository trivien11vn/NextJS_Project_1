export const metadata = {
    title: 'View Detail Blog',
    description: 'Blog Listsssss',
}

export default function ViewDetailLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <>
            {children}
        </>
    )
}
